0.4.2 (build 143) — consistent slider styling.

Small visual fix: the sliders in Settings drew their filled track in two different colors — most were green, but a few (Quarantine window, iCloud download limit) filled white, so one screen looked like it had two slider styles. Every slider now uses the same active-green fill. No behavior change.

To test: Settings → Safety & limits — confirm the Quarantine window, iCloud download limit, and Never-touch ceiling sliders all fill with the same green.
