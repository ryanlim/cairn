Followup on the Settings reshape: Photos Access and Indexing Scope are about the same thing (what cairn can see), so Photos Access moves into Library alongside Background Refresh. With those gone from Notifications, the category doesn't pull its weight — dropped. Its rows fan out where they actually belong: Alert on aborted run and Backlog alert threshold to Safety & limits (they're trip indicators for the rails); Verbose journal to Data & recovery (it controls journal data).

Root list is now 7 categories: Connection, Library, Safety & limits, Appearance, Data & recovery, Advanced, About.

The Library parent row also now surfaces a degraded Photos-auth state inline ("Photos: limited" or "Photos: denied"), since that blocks everything else cairn does on the library — should be obvious without drilling in.
