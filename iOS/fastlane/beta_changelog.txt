Fix: the connection latency shown on Settings → Connection now refreshes when you open the page and after every successful sync. Previously it was measured once at app boot (and refreshed only by the manual "Retry" banner tap), so the value could be hours stale on a long-running session. Same single-ping measurement under the hood; just measured more often.
