UI: fix a "scope of work disappeared" gap during the initial scan handoff between trust-seeding and hashing. The X/Y counter at the top of the initial-scan screen was briefly showing "0 / 0" between the imputation pass completing and the first hash tick. Caused by the imputation's syncProgress reset using total=0 instead of the phone library count; fixed by pinning total to the actual library size through that handoff. Counter now stays monotonic and meaningful end-to-end.
