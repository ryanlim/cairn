The connection status pill on Settings → Connection now shows when the latency was measured. "● healthy · 42ms · 12s ago" — the trailing tail advances over time (just now / Ns / Nm / Nh / Nd) so a number that looks fresh stays honest about its age. Updates on a 1-minute timer while the page is visible, and re-measures on appear + after every sync (from build 88).
