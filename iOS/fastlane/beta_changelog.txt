Copy sweep on the session-auth surfaces. The Settings → Advanced row no longer says "Required for /sync/*"; it now reads "Required for incremental sync" when signed out and "Signed in to Immich" / "Sign out" once signed in. The toggle subtitle drops the "sync/stream change-data-capture endpoint" framing for a plain-language version about streaming changes vs refetching the whole library. The sheet's explainer and the Status banner that surfaces on session expiry both got the same treatment. No behavior changes — just less jargon.
