Fix: destructive-action and file-picker dialogs on sub-pages now appear immediately instead of waiting until you pop back to the Settings root. Affects Export data, Import data, Reset index, Clear journal, Clear hash cache, Rescan library, Re-hash imputed entries, Sign out, Clear excluded assets, and the About sheet. Same bindings under the hood; the modifiers were attached to the root content (out of view while a sub-page is pushed) and are now attached to the NavigationStack itself so they present from any depth.
