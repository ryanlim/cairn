Accessibility floor for the destructive workflow: Settings toggle rows now announce their full label + sub-description in VoiceOver (was just "switch on / switch off"). Progress bars during sync and initial scan announce their percent value as they tick. Sync phase transitions ("Preparing", "Hashing", "Reconciling", "Finalizing", "Sync complete") are spoken via polite VoiceOver announcements instead of going silent. Per-row Trash / Exclude buttons on the Pending Review screen and the Reveal / Copy buttons on the API-key row in Settings now meet Apple's 44pt tap-target minimum without visually changing. The zoom-thumbnail overlay traps VoiceOver focus to the overlay until dismissed. Light-mode muted text bumped darker to clear WCAG AA contrast (was failing at 4.18:1 and 2.22:1; now well above the 4.5:1 bar).

Engine cleanup: the journal's per-source attribution field was renamed in code from `confirmedFromPhotoKit` to `confirmedFromChangeLog` — same wire-format on disk (existing journal files still decode), but the Swift identifier now reflects the portable concept rather than baking in iOS-specific terminology. CLI output for `cairn journal show` reads `confirmedFromChangeLog=N`. Journal tail rows show `conf-cl=N` in the in-app feed instead of `conf-pk=N`.

Documentation: the master design doc has been replaced by `ARCHITECTURE.md` at the repo root. The original `immich-ios-deletion-sync-plan.md` is archived under `docs/history/` with a header noting where the architecture pivoted during construction (in particular, the abandoned `PHAssetCollectionSubtype.smartAlbumRecentlyDeleted` approach is now correctly framed as a non-existent API). `CLAUDE.md` slimmed down to a working-session crib. README gains a "Is this safe to try" paragraph high up. PR template added. No user-facing app changes from this slice — repo polish for contributors landing from the Reddit launch.
