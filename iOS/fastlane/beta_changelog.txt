Counter for "Matching from server" + ongoing resource-pick diagnostic.

Counter: during the Matching from server phase, the trust-seeded count now ticks live as setImputed writes complete (every 50 entries). Previously the imputed count stayed at 0 until the entire pass finished, then jumped to the total — which made the phase feel like it was sitting still even on libraries where it was making real progress.

Diagnostic logging (carried over from build 100): every PHAsset enumerated during imputation gets inspected for resource-type ambiguity (both .video AND .fullSizeVideo, or both .photo AND .fullSizePhoto). Logs via Logger.notice AND NSLog so they're visible in both Console.app (subsystem app.cairn.ios, category resource-pick) and the legacy syslog stream (idevicesyslog -m "cairn-ambiguity").

No behavior change to identity/hashing logic.
