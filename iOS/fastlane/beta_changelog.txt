Diagnostic build (round 2): logs from imputation path + NSLog for syslog visibility.

Changes from build 99:
1. The ambiguity check now also fires from the imputation pass (where every PHAsset is enumerated once at the start to gather filename + fileCreatedAt). This means the 5 phantom .MOVs get inspected even when imputation matches them server-side and bypasses local hashing.
2. Added NSLog alongside Logger so the diagnostic also lands in legacy syslog (visible to idevicesyslog), not just the unified-logging stream (which only Console.app filters reliably surface).

To capture: clear hash cache + sync (toggle on or off, doesn't matter — imputation pass runs regardless). Stream device logs with `idevicesyslog -m cairn-ambiguity` or filter Console.app on "cairn-ambiguity" / "resource-pick".

No behavior change. Observation-only.
