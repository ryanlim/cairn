Diagnostic build: investigating the standalone-.MOV phantom-quarantine bug.

Adds a debug log when cairn enumerates an asset that has both .video and .fullSizeVideo resources (or both .photo and .fullSizePhoto). Logs each resource's type, isCurrent flag, and byte size, so we can confirm whether cairn and Immich are picking different resources for the same asset.

Hypothesis: for the 5 phantom .MOVs reported on build 97, the .isCurrent flag is set on .video while cairn currently always prefers .fullSizeVideo. Immich's mobile native hasher AND its upload path (via photo_manager.originFile) both honor .isCurrent first. If cairn picks .fullSizeVideo while Immich uploads .video, the server's stored SHA1 won't match cairn's local SHA1 — and imputation surfaces that mismatch as a phantom in ObservedStore. Logs will be visible via Console.app or idevicesyslog filtered on subsystem "app.cairn.ios" and category "resource-pick".

No behavior change. No fix yet.
