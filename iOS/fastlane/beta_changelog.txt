Migration confirmation: end-user report on build 122 shows the 19 stuck quarantine entries resolved — engine output transitioned `aliveOnPhone=6, pending=19` → `aliveOnPhone=25, pending=0` exactly when the resfn migration completed.

Fixing the "On iPhone" count toggle reported alongside that. Two functions read the phone library through different `PHAsset.fetchAssets` filters:

- `refreshLibrarySizeStats` (called at sync-start, bootstrap, action handlers) used `.typeUserLibrary` only with `includeHiddenAssets = false`. Sets `library.local` to the narrow count.
- `performLiveReconciliation`'s `visibleFetch` (since build 116) uses `[.typeUserLibrary, .typeCloudShared, .typeiTunesSynced]` with `includeHiddenAssets = true`. Sets `library.local` to the broader count mid-sync.

UI then oscillated between the two values every sync round-trip: small at sync-start, larger after the engine pass, small at the next sync-start, repeat.

This build unifies both fetches on the broader filter. The "On iPhone" stat now reflects every phone asset cairn considers for the alive-on-phone safety check, which is what the "alive on the device that cairn could potentially manage" semantics call for. The narrow-filter behavior was a leftover from before the build-116 widening — only this stat path hadn't been updated.

No other visible behavior change. The broader count is the correct one going forward.
